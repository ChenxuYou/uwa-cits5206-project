# Build output. The root .gitignore covers local databases with *.db — this file
# deliberately does not enumerate database filenames, because the enumeration went
# stale the moment the development store moved past ric-costing-v3.db.
bin/
obj/
